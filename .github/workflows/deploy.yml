# Publica la web en GitHub Pages en cada push a main.
name: Desplegar en GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  construir:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: diseno-laplebe
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: diseno-laplebe/package-lock.json

      - run: npm ci

      # El sitio cuelga de /la-plebe-latinoamerica, no de la raíz del dominio.
      - run: npm run build
        env:
          NEXT_PUBLIC_BASE_PATH: /la-plebe-latinoamerica

      # Sin esto GitHub Pages ignora las carpetas que empiezan por guion bajo.
      - run: touch out/.nojekyll

      - uses: actions/upload-pages-artifact@v3
        with:
          path: diseno-laplebe/out

  desplegar:
    needs: construir
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
